<!doctype html>
<html lang="{{ request.locale.iso_code }}">
  <head>
    {% # Inlined CSS Variables %}
  {% render 'css-variables' %}

    {% # Load Custom tailwind css %}
  {{ 'app.css' | asset_url | stylesheet_tag: preload: true }}

    {% # Load and preload the critical CSS %}
    {{ 'critical.css' | asset_url | stylesheet_tag: preload: true }}


    {% # Load Custom Alpine js %}
    <script src="{{ 'alpine@3.15.0.min.js' |  asset_url }}" defer>
  </script>

    {% # Social, title, etc. %}
    {% render 'meta-tags' %}

    {{ content_for_header }}
  </head>

  <body>
    {% sections 'header-group' %}

    {{ content_for_layout }}

    {% sections 'footer-group' %}


    {% # Load Custom Swiper js %}
    <script src="{{ 'swiper-element-bundle@3.13.0.min.js' |  asset_url }}" defer></script>

    {% # Load Custom Gsap js %}
    <script src="{{ 'gsap@3.13.min.js' |  asset_url }}" defer></script>

    {% # Load Custom App js %}
    <script src="{{ 'app.js' |  asset_url }}" defer></script>
  </body>
</html>